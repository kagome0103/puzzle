num = 11
while True:
    if str(num) == str(num)[::-1] and \
            format(num, 'b') == format(num, 'b')[::-1] and \
            format(num, 'o') == format(num, 'o')[::-1]:
        print(num)
        break

    num += 1

# 文字列の反転
# 基数変換
# and処理の書き方
# 改行の仕方、改行コードの表示

# コードの整形
